let greeting: string = "Hello World";

console.log(greeting.toLowerCase());

// number

let userId: number = 23344; // 233.34 also a number

// boolean

let isUserLoggedIn: boolean = false;
export {};

let arr = [1, 2, 3, "hello"];

// tuple
let arr1: [number, string] = [2, "raj"]; // this is promiss i give u only
let arr2: [string, number] = ["raj", 1]; // but i give so it is error like

// enum
// an enum in TypeScript is a special type that defines a set of named constants to make code more readable, type-safe, and self-documenting.
enum Status {
  Pending = "PENDING",
  InProgress = "IN_PROGRESS",
  Completed = "COMPLETED",
  Failed = "FAILED",
}


